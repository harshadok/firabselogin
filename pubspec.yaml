name: api_fech
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  smooth_page_indicator: ^1.0.0+2
  firebase_auth: ^4.3.0
  either_dart: ^0.3.0
  bloc: 8.1.0 
  flutter_bloc: 8.1.1 
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - images/
